# THFC Dashboard automation — API key config
#
# 1. Copy this file to ".env" (remove ".example")
# 2. Replace the placeholder below with your REAL Anthropic API key
# 3. NEVER commit .env to GitHub, NEVER paste your real key into chat
#    (.env is already excluded via .gitignore below)

ANTHROPIC_API_KEY=sk-ant-YOUR-KEY-HERE
